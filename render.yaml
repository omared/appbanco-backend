services:
  - type: web
    name: appbanco-backend
    runtime: node
    plan: free
    buildCommand: npm ci && npm run build
    startCommand: npm start
    envVars:
      - key: ALLOWED_ORIGIN
        value: https://omared.github.io
      - key: NODE_VERSION
        value: 20.18.0
